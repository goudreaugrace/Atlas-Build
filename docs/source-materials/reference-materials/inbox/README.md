# Inbox

Drop unsorted source/reference files here when speed matters.

If there is more than one related file, create a dated folder:

`YYYY-MM-DD-short-topic/`

After triage, move the bundle into one of the durable lanes:

- `../ali-dumps/`
- `../external-frameworks/`
- `../source-reports/`

Keep raw files unchanged. Add or update manifest metadata after moving.
