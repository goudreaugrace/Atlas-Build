# Raw Data and Reference Assets

These files are included for Codex/reference. The prototype uses processed JSON in `src/data/`.

Do not load raw PowerPoint, PDF, or Excel files in the browser.

Use scripts or future backend services to parse/ingest raw assets.
